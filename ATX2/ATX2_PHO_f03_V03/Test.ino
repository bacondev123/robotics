
void test_func() {
  // track_CL(20);
  // trackLine(20);
  // trackLine_PID(20, 4, 0, 0.005);
  // trackLineTime_PID_Static(20, 5000);
  // spinRight_SensorLineSkip(30, 3, 4);
  // walkCross_CustomSpeed(40,5000);
  // spinLeft_SensorLineSkip0(20, 1, 2);
  // RunPath("2R2");
  // walkCross_CustomSpeed(50,1000);
  // delay(1000);
  // trackLineTime_PID_Acceleration(40,5000);
  // trackCross_PID_Static();
  // skipCross_PID_Static(1);
  // trackCross_PID_Static();
  // delay(500);
  // walkCross_StaticSpeed(500);
}

void mission_test() {
  // walkCross_CustomSpeed(20, 500);
  // trackCross_PID_Static();

  // RunPath("S");
  // RunPath("S S 2L1 T");
  // keep_down();
  // RunPath("R2 S w 2R2 S S");
  // keep_up();
  // RunPath("2R2 S S 2R2 T");
  // keep_down();
  // RunPath("L1 S w 2L1 S S");
  // keep_up();
  // RunPath("2R2 S S S L1 S0");
  // keep_down();
  // RunPath("2R2 S3 R2 S S S");
  // keep_up();
  // RunPath("2R2 S S S R2 S3");
  // keep_down();
  // RunPath("2L1 S0 L1 S S S");


  // for (int i = 1; i <= 5; i++) {
  //   beep();
  //   delay(100);
  // }


  // trackLineTime_PID_Acceleration(50,5000);
}
